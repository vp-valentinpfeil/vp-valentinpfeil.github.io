<style>
/* Ensure brand is visible in overlay header */
.site-header .navbar-brand,
.header--overlay .navbar-brand,
.header--overlay .navbar .navbar-brand,
.header--overlay a.navbar-brand {
  opacity: 1 !important;
  visibility: visible !important;
}

/* Don’t squash the logo */
.navbar-brand img,
.navbar-brand svg {
  height: 40px;
  width: auto;
  display: inline-block;
  vertical-align: middle;
}

/* Hide “Your Name” fallback if it appears somewhere else */
.navbar-brand .brand__text,
.brand .brand__text {
  display: none !important;
}
</style>